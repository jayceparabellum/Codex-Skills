interface:
  display_name: "Correct"
  short_description: "Deep correctness audit of recent changes. Verifies every change is logically sound, consistent, and introduces no reg..."
  default_prompt: "Use $correct for this workflow."

interface:
  display_name: "Scope Lock"
  short_description: "Freeze task scope into must-have, should-have, and later."
  default_prompt: "Use scope-lock to define what is in scope, out of scope, and deferred before execution."
